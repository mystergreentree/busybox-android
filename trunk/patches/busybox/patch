diff --git a/Makefile b/Makefile
index 9648c9c..6bb8117 100644
--- a/Makefile
+++ b/Makefile
@@ -1,8 +1,8 @@
 VERSION = 1
 PATCHLEVEL = 19
 SUBLEVEL = 3
-EXTRAVERSION =
-NAME = Unnamed
+EXTRAVERSION = vintozver
+NAME = Vintozver
 
 # *DOCUMENTATION*
 # To see a list of typical targets execute "make help"
diff --git a/Makefile.flags b/Makefile.flags
index b3e1371..ed71e86 100644
--- a/Makefile.flags
+++ b/Makefile.flags
@@ -97,7 +97,7 @@ CFLAGS += $(strip $(subst ",,$(CONFIG_EXTRA_CFLAGS)))
 #"))
 endif
 
-LDLIBS += m crypt
+LDLIBS += m
 
 ifeq ($(CONFIG_PAM),y)
 LDLIBS += pam pam_misc
diff --git a/coreutils/date.c b/coreutils/date.c
index 6a7d5fa..4eca7bd 100644
--- a/coreutils/date.c
+++ b/coreutils/date.c
@@ -30,6 +30,13 @@
 //config:	  date is used to set the system date or display the
 //config:	  current time in the given format.
 //config:
+//config:config FEATURE_DATE_SET
+//config:	bool "Enable system date/time setting (-s)"
+//config:	default y
+//config:	depends on DATE
+//config:	help
+//config:	  Enable system date/time setting option (-s).
+//config:
 //config:config FEATURE_DATE_ISOFMT
 //config:	bool "Enable ISO date format output (-I)"
 //config:	default y
@@ -159,7 +166,9 @@ static void maybe_set_utc(int opt)
 static const char date_longopts[] ALIGN1 =
 		"rfc-822\0"   No_argument       "R"
 		"rfc-2822\0"  No_argument       "R"
+		#if ENABLE_FEATURE_DATE_SET
 		"set\0"       Required_argument "s"
+		#endif
 		"utc\0"       No_argument       "u"
 	/*	"universal\0" No_argument       "u" */
 		"date\0"      Required_argument "d"
@@ -289,10 +298,12 @@ int date_main(int argc UNUSED_PARAM, char **argv)
 
 		maybe_set_utc(opt);
 
+		#if ENABLE_FEATURE_DATE_SET
 		/* if setting time, set it */
 		if ((opt & OPT_SET) && stime(&ts.tv_sec) < 0) {
 			bb_perror_msg("can't set date");
 		}
+		#endif
 	}
 
 	/* Display output */
diff --git a/coreutils/stat.c b/coreutils/stat.c
index 2797719..93dc61d 100644
--- a/coreutils/stat.c
+++ b/coreutils/stat.c
@@ -99,9 +99,6 @@ static const char *file_type(const struct stat *st)
 	if (S_ISFIFO(st->st_mode)) return "fifo";
 	if (S_ISLNK(st->st_mode))  return "symbolic link";
 	if (S_ISSOCK(st->st_mode)) return "socket";
-	if (S_TYPEISMQ(st))        return "message queue";
-	if (S_TYPEISSEM(st))       return "semaphore";
-	if (S_TYPEISSHM(st))       return "shared memory object";
 #ifdef S_TYPEISTMO
 	if (S_TYPEISTMO(st))       return "typed memory object";
 #endif
diff --git a/examples/android-build b/examples/android-build
index f5fe49b..1149090 100755
--- a/examples/android-build
+++ b/examples/android-build
@@ -7,10 +7,7 @@
 # the must be patched around) and Gingerbread.
 
 # Point this to the Android root directory; it's used in the defconfig CFLAGS
-export A="$HOME/android"
-
-# Android product being built
-P=zoom2
+export A="/var/data/work/android-src"
 
 # Toolchain version in use by this version of Android
 GCCVER=4.4.3
@@ -20,11 +17,11 @@ export PATH="$A/prebuilt/linux-x86/toolchain/arm-eabi-$GCCVER/bin:$PATH"
 # Set the linker flags; compiler flags are in the defconfig file
 if grep "^CONFIG_STATIC=y" .config >/dev/null ; then
 	# Static linking
-	LDFLAGS="-static -Xlinker -z -Xlinker muldefs -nostdlib $A/out/target/product/$P/obj/lib/crtbegin_static.o $A/out/target/product/$P/obj/lib/crtend_android.o -L$A/out/target/product/$P/obj/lib -L$A/out/target/product/$P/obj/STATIC_LIBRARIES/libm_intermediates -L$A/out/target/product/$P/obj/STATIC_LIBRARIES/libc_intermediates"
+	LDFLAGS="-static -Xlinker -z -Xlinker muldefs -nostdlib $A/prebuilt/ndk/android-ndk-r6/platforms/android-9/arch-arm/usr/lib/crtbegin_static.o $A/prebuilt/ndk/android-ndk-r6/platforms/android-9/arch-arm/usr/lib/crtend_android.o -L$A/prebuilt/ndk/android-ndk-r6/platforms/android-9/arch-arm/usr/lib"
 	LDLIBS="m c gcc"
 else
 	# Dynamic linking
-	LDFLAGS="-Xlinker -z -Xlinker muldefs -nostdlib -Bdynamic -Xlinker -T$A/build/core/armelf.x -Xlinker -dynamic-linker -Xlinker /system/bin/linker -Xlinker -z -Xlinker nocopyreloc -Xlinker --no-undefined $A/out/target/product/$P/obj/lib/crtbegin_dynamic.o $A/out/target/product/$P/obj/lib/crtend_android.o -L$A/out/target/product/$P/obj/lib"
+	LDFLAGS="-Xlinker -z -Xlinker muldefs -nostdlib -Bdynamic -Xlinker -T$A/build/core/armelf.x -Xlinker -dynamic-linker -Xlinker /system/bin/linker -Xlinker -z -Xlinker nocopyreloc -Xlinker --no-undefined $A/prebuilt/ndk/android-ndk-r6/platforms/android-9/arch-arm/usr/lib/crtbegin_static.o $A/prebuilt/ndk/android-ndk-r6/platforms/android-9/arch-arm/usr/lib/crtend_android.o -L$A/prebuilt/ndk/android-ndk-r6/platforms/android-9/arch-arm/usr/lib"
 	LDLIBS="dl m c gcc"
 fi
 
diff --git a/libbb/xfuncs_printf.c b/libbb/xfuncs_printf.c
index 56ee459..88f3a26 100644
--- a/libbb/xfuncs_printf.c
+++ b/libbb/xfuncs_printf.c
@@ -538,6 +538,7 @@ int FAST_FUNC bb_xioctl(int fd, unsigned request, void *argp)
 }
 #endif
 
+/*
 char* FAST_FUNC xmalloc_ttyname(int fd)
 {
 	char *buf = xzalloc(128);
@@ -548,6 +549,7 @@ char* FAST_FUNC xmalloc_ttyname(int fd)
 	}
 	return buf;
 }
+*/
 
 void FAST_FUNC generate_uuid(uint8_t *buf)
 {
diff --git a/miscutils/ionice.c b/miscutils/ionice.c
index bd30060..a881504 100644
--- a/miscutils/ionice.c
+++ b/miscutils/ionice.c
@@ -20,12 +20,12 @@
 
 static int ioprio_set(int which, int who, int ioprio)
 {
-	return syscall(SYS_ioprio_set, which, who, ioprio);
+	return syscall(__NR_ioprio_set, which, who, ioprio);
 }
 
 static int ioprio_get(int which, int who)
 {
-	return syscall(SYS_ioprio_get, which, who);
+	return syscall(__NR_ioprio_get, which, who);
 }
 
 enum {
